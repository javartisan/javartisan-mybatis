<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.javartisan.mysql2.mapper.DogMapper">


    <select id="findById" resultMap="dogMap">
    SELECT  dog_name, type FROM  dogtab WHERE id=#{id}
    </select>

    <insert id="saveDog">
    INSERT  INTO  dogtab(dog_name, type) VALUES (#{dog.dogName},#{dog.type});
    </insert>

    <resultMap id="dogMap" type="dog">
        <result property="dogName" column="dog_name"/>
    </resultMap>

</mapper>